# Frogger 移植計画

## 概要

Godot/GDScript 製の [FroggerTutorial](https://github.com/wojciech-bilicki/FroggerTutorial) を、
既存の Flix ゲームエンジン（SceneTree + LWJGL）に移植する。

現在のコードベースは "Dodge the Creeps" という別のゲームが実装されている。
エンジン層（`Engine.flix`, `SceneTree.flix`, `LwjglLayer.flix`, `NodeBuilders.flix`, `Vec2D.flix` 等）はそのまま活用し、
ゲーム層（`Game.flix`, `src/scene/*.flix`, テクスチャ/サウンド、テスト）を Frogger 用に作り替える。

## 元ゲームの仕様

| 項目 | 値 |
|------|-----|
| 画面サイズ | 784 x 1000 |
| セルサイズ | 64px（移動単位） |
| スプライト原寸 | 約16x16px（4倍スケール） |
| 入力 | WASD / 矢印キー（**単発入力**＝1回押す=1マス移動） |
| ライフ | 初期値設定可能（デフォルト1） |
| 制限時間 | 30秒（カウントダウン） |

### ゲームボード構成（上から下）

```
┌──────────────────────────┐  y=0
│     ホームスロット (5箇所)   │  ~64px
├──────────────────────────┤
│     水域 (6レーン)          │  6 x 64 = 384px
│  - 丸太レーン x4            │
│  - 亀レーン x2              │
├──────────────────────────┤
│     中間歩道                │  64px
├──────────────────────────┤
│     道路 (5レーン)          │  5 x 64 = 320px
│  - 車レーン x5              │
├──────────────────────────┤
│     下歩道 (開始地点)       │  64px
└──────────────────────────┘  y=~960px
```

### ゲームメカニクス

- **車**: レーン内を一定速度で水平スクロール。端で反対側にワープ。接触=即死
- **丸太**: 水面をスクロール。乗ると一緒に移動。水に落ちると溺死
- **亀**: 丸太と同様のプラットフォーム（複数匹でセクション構成）
- **ホームスロット**: 5箇所のゴール。全て埋めるとクリア
- **死亡**: 死亡スプライト表示 → ライフ減少 → 開始地点にリスポーン
- **タイマー**: 30秒カウントダウン。0になると死亡

## 技術的な課題と対策

### 1. 単発入力 (Just Pressed)

現在の `Engine.Game.isKeyPressed` はキーが押されている間ずっと `true` を返す。
Frogger ではキー1回押し=1マス移動が必要。

**対策**: `GameState` に前フレームの押下キー集合を保持し、エッジ検出する。

```
isJustPressed(key) = isKeyPressed(key) AND NOT prevKeys.contains(key)
```

### 2. プラットフォーム乗車

プレイヤーが丸太/亀の上にいるとき、プラットフォームと一緒に移動する。

**対策**: 毎フレーム、プレイヤー位置と全丸太/亀の矩形を比較。
重なっていれば、そのフレームのプラットフォーム移動量をプレイヤー位置に加算。

### 3. 水域判定

プレイヤーが水域にいて、かつ丸太/亀の上にいなければ溺死。

**対策**: プレイヤーの Y 座標が水域範囲内かどうかで判定。
水域内なら丸太/亀との重複チェック。重複なし=溺死。

### 4. スプライト回転

カエルは進行方向に応じて回転する（上=0°, 右=90°, 下=180°, 左=270°）。

**対策**: Sprite2D に `rotation` フィールドを追加してレンダリング時にシェーダーで回転。
ただし最初のフェーズでは flipH/flipV で代用し、後から改善する。

→ 実装方針: 上向き/下向きは flipV で対応、左右は横向きスプライトを別に用意するか、
最初は回転なしで進めて Phase 9 で仕上げる。

### 5. 背景テクスチャのタイル描画

道路・水面は画面幅いっぱいにタイルで繰り返す必要がある。

**対策**: 各レーンに横幅いっぱいの Sprite2D を配置し、UV スケールで繰り返す。
または単色の色付き矩形で代用（フェーズ2で色付き → フェーズ9でテクスチャ化）。

---

## 作業フェーズ

### - [x] Phase 1: プロジェクトセットアップ
- [x] `/tmp/FroggerTutorial/Assets/` からゲーム用スプライトを `textures/` にコピー
- [x] `Game.flix` を書き替え: 画面サイズ (784x1000)、テクスチャマニフェスト、オーディオマニフェスト（空でOK）
- [x] 旧ゲーム用シーンファイル (`MainScene.flix`, `PlayerScene.flix`, `MobScene.flix`, `HUDScene.flix`) を削除
- [x] `FroggerScene.flix` を作成: 空のルートノードだけ返す最小シーン
- [x] 旧テスト (`test/*.flix`) のうちシーン依存のものを削除/コメントアウト
- [x] `flix test` が通ることを確認
- [x] `flix run` でウィンドウが開くことを確認

**確認ポイント**: 正しいサイズの空ウィンドウが表示される

---

### - [x] Phase 2: ゲームボード（静的背景）
- [x] レイアウト定数を定義（各ゾーンの Y 座標、レーン数、セルサイズ等）
- [x] 背景ゾーンを色付き矩形で描画:
  - 水域 = 青 (`{r=0.0, g=0.2, b=0.6}`)
  - 道路 = 暗灰 (`{r=0.15, g=0.15, b=0.15}`)
  - 歩道 = 緑 (`{r=0.2, g=0.5, b=0.2}`)
  - ホーム = 緑
- [x] `flix run` で色分けされたゲームボードが表示される

**確認ポイント**: 上からホーム(緑)→水域(青)→歩道(緑)→道路(灰)→歩道(緑) のレイアウト

---

### - [x] Phase 3: プレイヤー（グリッド移動）
- [x] `FroggerPlayer.flix` を作成
- [x] 単発入力検出ロジック (`GameState` に `prevKeys: Set[Engine.Key]` 追加)
- [x] カエルスプライトの表示（FroggerIdle.png）
- [x] グリッド移動: 1回のキー入力で 64px 移動（lerp 補間）
- [x] 画面端のクランプ（ボード外に出られない）
- [x] `flix run` でカエルが上下左右にホップする

**確認ポイント**: WASD/矢印でグリッド単位の移動。端で止まる

---

### - [ ] Phase 4: 車レーン
- [ ] `VehicleLane.flix` を作成
- [ ] 車両の水平スクロール（RigidBody2D or Area2D + velocity）
- [ ] 画面端でワープ（反対側に再出現）
- [ ] 5レーン分のデータ定義（速度、方向、車種、台数、間隔）
- [ ] `flix run` で5レーンに車が流れる

**確認ポイント**: 各レーンで異なる車が異なる速度・方向でスクロール

---

### - [ ] Phase 5: 車との衝突 & 死亡
- [ ] プレイヤー (Area2D) と車 (Area2D) の衝突判定
- [ ] 衝突時: 死亡スプライト (FroggerDead.png) 表示 → 一定時間後リスポーン
- [ ] ライフシステム（`GameState` に `lives: Int32` 追加）
- [ ] ライフ 0 でゲームオーバー
- [ ] `flix run` で車に轢かれて死亡→リスポーンが動作する

**確認ポイント**: 車にぶつかると死亡、リスポーン後に再プレイ可能

---

### - [ ] Phase 6: 丸太レーン（水域の基本）
- [ ] `WaterLane.flix` を作成
- [ ] 丸太の構成: LogStart + LogMiddle(n個) + LogEnd をまとめた1ノード
- [ ] 丸太の水平スクロール
- [ ] プラットフォーム乗車判定（プレイヤーと丸太の矩形重複）
- [ ] 乗車中: プレイヤーが丸太と一緒に移動
- [ ] 水域判定: 水域内 & プラットフォーム上にいない → 溺死
- [ ] `flix run` で丸太に乗って水域を横断できる

**確認ポイント**: 丸太に乗れる。水に落ちると死亡

---

### - [ ] Phase 7: 亀レーン
- [ ] `TurtleLane.flix` を作成（WaterLane と類似構造）
- [ ] 亀セクション: 複数の亀スプライトでプラットフォームを構成
- [ ] 丸太と同様のスクロール＆乗車ロジック
- [ ] `flix run` で亀に乗って水域を横断できる

**確認ポイント**: 亀も丸太と同じくプラットフォームとして機能する

---

### - [ ] Phase 8: ホームスロット & 勝利条件
- [ ] `HomeSlot.flix` を作成
- [ ] 5箇所のホームスロットを配置
- [ ] プレイヤーがホームスロットに到達 → HomeFrog.png 表示、プレイヤーリセット
- [ ] 全5箇所埋まったら勝利
- [ ] `flix run` で全ホームスロットを埋めてクリアできる

**確認ポイント**: ゴールに到達すると埋まる。5つ全部でクリア

---

### - [ ] Phase 9: HUD & ゲームフロー完成
- [ ] ライフ表示（画面上部にカエルアイコン × 残機）
- [ ] カウントダウンタイマー（30秒。0でプレイヤー死亡）
- [ ] スコア表示（任意）
- [ ] ゲームオーバー画面 ("You Lost!")
- [ ] 勝利画面 ("You Won!")
- [ ] リスタート機能（ボタンまたは Enter キー）
- [ ] スプライト回転の実装（シェーダー修正 or 回転済みスプライト用意）
- [ ] 背景テクスチャ化（色付き矩形 → 実際のテクスチャ）
- [ ] `flix run` で完全なゲームループが動作する

**確認ポイント**: 開始→プレイ→死亡/クリア→リスタート の一連のフロー

---

## ファイル構成（移植後）

```
src/
├── Main.flix              # エントリポイント（ほぼ変更なし）
├── Game.flix              # GameState, マニフェスト, ゲームループ ← 全面書き替え
├── Engine.flix            # 変更なし
├── SceneTree.flix         # 変更なし（必要に応じてバグ修正のみ）
├── LwjglLayer.flix        # 変更なし（Phase 9 でスプライト回転対応するかも）
├── NodeBuilders.flix      # 変更なし
├── Vec2D.flix             # 変更なし
├── TextLayout.flix        # 変更なし
├── AudioUtil.flix         # 変更なし
├── RandomUtil.flix        # 変更なし
└── scene/
    ├── FroggerScene.flix   # メインシーン（ボード構築、イベントハンドリング）
    ├── FroggerPlayer.flix  # プレイヤー（グリッド移動、死亡、リスポーン）
    ├── VehicleLane.flix    # 車レーン（スクロール、ワープ）
    ├── WaterLane.flix      # 丸太レーン（スクロール、プラットフォーム）
    ├── TurtleLane.flix     # 亀レーン
    ├── HomeSlot.flix       # ホームスロット
    └── FroggerHUD.flix     # HUD（ライフ、タイマー、メッセージ）
```

## アセット

`/tmp/FroggerTutorial/Assets/` から以下をコピー:

| ファイル | 用途 |
|----------|------|
| FroggerIdle.png | カエル（静止） |
| FroggerLeap.png | カエル（ジャンプ中） |
| FroggerDead.png | カエル（死亡） |
| HomeFrog.png | ゴールに到達したカエル |
| Car1.png ~ Car5.png | 車 5種 |
| LogStart.png | 丸太（左端） |
| LogMiddle.png | 丸太（中間、タイル） |
| LogEnd.png | 丸太（右端） |
| Turtle_01~03.png | 亀 3フレーム |
| Road.png | 道路背景 |
| Water.png | 水面背景 |
| Median.png | 中央分離帯 |
| Home.png | ホームエリア背景 |

## 注意事項

- 各フェーズ完了後に `flix test` と `flix run` を実行して動作確認すること
- エンジン層 (`SceneTree.flix`, `LwjglLayer.flix` 等) は原則変更しない
- 必要に応じて NodeBuilder を追加することは OK
- 旧ゲーム (Dodge the Creeps) のコードは不要になったら削除してよい
